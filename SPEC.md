# JLPT 漢字学習ツール 仕様書

**作成日：2026年7月27日**　**最終更新：2026年7月27日（合言葉機能の追加を反映）**
**対象システム：** Kanji JLPT N5–N3（技能実習生向け 漢字学習ウェブアプリ）
**公開URL：** https://owlwindkaze-ux.github.io/owltiger1021/
**リポジトリ：** https://github.com/owlwindkaze-ux/owltiger1021
**ブランチ：** `claude/japanese-n3-kanji-AaKYe`

---

## 1. システム概要

日本で働くインドネシア人技能実習生が、日本語能力試験（JLPT）N5〜N3レベルの漢字を、
**読み・意味（インドネシア語）・用例・書き順アニメーション**で自習できるウェブアプリ。

| 項目 | 内容 |
|---|---|
| 収録漢字 | 746字（N5 102・N4 144・N3 367・**介護漢字 133**） |
| 収録語彙 | **4,558語**（N5 709・N4 662・N3 2,078・介護 227・**記録 757**・現場 125）＋施設独自の追加分（`vocab-extra.csv`。現在1語で、アプリの表示は4,559語）。漢字語・かな語・カタカナ語を網羅 |
| 用例 | 2,435語（ふりがな・意味・JLPTレベル付き、1字あたり平均4語） |
| 表示言語 | 日本語＋インドネシア語（英語に切替可） |
| 動作環境 | Webブラウザ（PC・スマホ・タブレット）／インターネット接続が必要 |
| サーバー | GitHub Pages（無料・静的ホスティング） |
| 費用 | 0円 |
| 利用登録 | 不要（URLを開き、合言葉を入力するだけ） |
| 個人情報 | 収集しない（学習記録は利用者の端末内にのみ保存） |

### 1.1 設計方針

- **外部ライブラリ・CDN・APIを一切使わない**（`kanji.html` 単体で完結）。海外の回線でも軽く、リンク切れで壊れない
- **サーバー側処理なし**（静的ファイルのみ）。DB・ログイン・バックエンド不要で保守コストゼロ
- データ（JSON）とアプリ（HTML）を分離。漢字の追加・修正はJSONの編集だけで完結する

---

## 2. 機能仕様

### 2.1 画面構成（4タブ）

| タブ | 表示名 | 機能 |
|---|---|---|
| ① 漢字一覧 | Kanji | 全漢字をカード表示。検索・絞り込み・並べ替え。カードをタップで詳細画面 |
| ①' 熟語 | Kosakata | 熟語3,053語を表示。レベル・分野で絞り込み、タップで構成漢字へ |
| ② フラッシュカード | Kartu | 暗記練習。4つの出題形式、覚えた／要復習の仕分け |
| ③ 書き順ガイド | Urutan Coretan | 書き順図（番号付き）とコマ送り図。文字タップで1画ずつアニメーション |
| ④ 学習状況 | Progres | レベル別の到達率、要復習リスト、記録の書き出し・消去 |

### 2.2 ①一覧タブ

| 機能 | 仕様 |
|---|---|
| 検索 | **当たり方に点数をつけ、よく当たった順に表示**。①検索語に漢字が含まれるとき＝**その漢字だけ**を出す（「水」で「泳」などの用例一致は出さない）②数字＝画数 ③かな＝読みが完全一致→先頭一致→途中一致 ④ローマ字＝意味が完全一致→**語頭一致**（`air` が `chair` に当たらない）。**直接1件も当たらないときだけ**、その字を使う熟語からも探す（`kaigo` → 介・護） |
| レベル絞り込み | すべて／N5／N4／N3 |
| 並べ替え | レベル順（やさしい順）／使用頻度順／画数少ない順／画数多い順 |
| 学習状態で絞り込み | すべて／未学習／要復習／覚えた |
| 表示件数 | 初回120字、「もっと表示」で+120字ずつ（描画負荷対策） |
| カード表示内容 | 漢字・レベル・音読み2件・訓読み2件・意味3件・画数・学習状態マーク（緑=覚えた／赤=要復習） |

### 2.3 詳細画面（カードタップで開く）

| 項目 | 仕様 |
|---|---|
| 書き順 | **開くと自動で1画ずつアニメーション再生**。文字または「▶再生」で再生し直し。再生後は番号付きの完成図を表示 |
| コマ送り図 | 1画目・2画目…と積み上がる小図を全画数ぶん表示（新しい画が赤） |
| 読み方 | 音読み（青）／訓読み（橙）を全件表示 |
| この字を使う言葉 | 辞書の用例と熟語データを**1つに統合**（重複を除去）。単語・ふりがな・意味（ID＋EN）・レベル。最大14語表示、タップで熟語の詳細へ |
| 部首 | 部首の文字・名称（日本語）・意味 |
| 属性 | レベル・画数・小学校の学年・使用頻度順位 |
| 学習状態 | 現在の状態（覚えた／要復習／未記録）をバッジで表示。ボタンは状態に応じて強調 |
| 操作 | 「この漢字を覚えた」「要復習」で登録。**同じボタンの再押下で取り消し**（モーダルは閉じずに状態のみ更新） |
| 一覧での表示 | カード右上に「✓ 覚えた」「↻ 要復習」のラベル、**見出し文字の色分け**（緑／赤）、カード背景の淡い色分け |
| 「この字を使う言葉」 | 各行を学習状態で色分け（覚えた＝緑文字＋緑の左帯、要復習＝赤文字＋赤の左帯、印は ✓ / ↻）。凡例を熟語タブに表示 |

### 2.4 ②フラッシュカードタブ

| 機能 | 仕様 |
|---|---|
| 出題形式 | 漢字→読み／漢字→意味／読み→漢字／意味→漢字 |
| 出題範囲 | レベル（すべて/N5/N4/N3）×状態（全部／未学習／要復習） |
| 出題順 | 開始時にランダムシャッフル |
| 操作 | カードをタップで裏返す →「覚えた」「まだ」で仕分け → 次の問題へ |
| 進捗表示 | 進捗バー、n/N、覚えた数、要復習数 |
| 終了時 | 正答率を表示。「もう一度」／「要復習だけやり直す」 |
| キーボード | `Space`/`Enter`＝答えを見る、`→`/`←`＝次/前、`1`＝まだ、`2`＝覚えた |

### 2.5 ③書き順ガイドタブ

| 機能 | 仕様 |
|---|---|
| 表示 | 漢字ごとにカード表示。番号付き書き順図＋全画数のコマ送り図＋読み |
| アニメーション | 図をタップすると1画ずつ順に書く（1画あたり0.24〜0.75秒、画の長さに比例） |
| 検索・絞り込み | 漢字・画数・読みで検索、レベル絞り込み、画数順の並べ替え |
| 表示件数 | 初回60字、「もっと表示」で+60字ずつ |

### 2.6 ④学習状況タブ

| 機能 | 仕様 |
|---|---|
| 到達率 | N5/N4/N3ごとに「覚えた字数／総字数」とバー表示 |
| 要復習リスト | 要復習に入れた漢字を一覧表示（タップで詳細へ） |
| 書き出し | 学習記録を `kanji-progress.json` としてダウンロード（バックアップ用） |
| 消去 | 確認ダイアログのうえ全記録を削除 |

### 2.6-b 熟語タブ（Kosakata）

| 機能 | 仕様 |
|---|---|
| 収録 | JLPT語彙2,928語 ＋ **介護・看護 201語** ＋ 一般現場125語（合計3,254語） |
| 介護の分野 | 介護（ケア）60／医療・症状56／**声かけ表現26**／記録・勤務22／施設・職種19／身体・部位18／認知症13／リハビリ10 |
| その他の分野 | 安全／生活・手続き／作業一般／製造・工場／品質 |
| 表示内容 | 熟語・ふりがな・意味（インドネシア語）・レベル・分野 |
| 検索 | 熟語・読み・意味（インドネシア語/英語）。漢字・かなは完全一致→先頭一致→途中一致、ローマ字は完全一致→語頭一致の順に点数をつけ、よく当たった順に表示 |
| 絞り込み | レベル（N5/N4/N3/介護/施設/現場）× **分野（大分類）** × **小分類**（3段階） |
| 小分類（介護201語を31区分） | 介護＝基本・利用者12／移動・移乗12／食事・嚥下12／排泄12／入浴・清潔8／口腔ケア2／皮膚・褥瘡2　医療＝症状17／薬・処置16／バイタル13／病気10　声かけ＝あいさつ5／体調をきく4／移動のとき5／食事のとき3／排泄のとき1／入浴・着替え3／報告・連絡5　記録＝記録・書類11／安全・権利7／勤務・シフト4　施設＝職種8／施設の種類7／施設内の場所4　身体＝上半身8／下半身6／体の組織4　認知症＝症状12／対応1　リハビリ＝訓練・専門職8／活動2 |
| 小分類セレクト | 選択中のレベル・分野に存在する小分類だけを件数つきで自動表示（2件未満なら非表示） |
| 熟語をタップ | 意味（ID＋EN）と**構成漢字のカード**を表示。漢字をタップすると書き順アニメへ |
| 画面遷移の履歴 | 漢字⇔熟語の行き来を履歴（navStack）で保持。左上の「← 戻る」で1つ前の画面に復帰。×は完全に閉じる。画面外タップ・Escも「戻る」として動作 |
| 漢字詳細から | 「この字を使う言葉」に統合表示（用例＋熟語、重複除去、最大14語）。タップで熟語画面へ |
| フラッシュカード | 出題対象を「漢字／熟語」で切替可能（4形式・レベル別・未学習／要復習） |
| 学習記録 | 漢字とは別に保存（`localStorage` キー `jlpt-kanji-progress-v1-vocab`）。一覧は「記録」プルダウン（すべて／未学習／要復習／覚えた）で絞り込める |

### 2.6-x 学習記録の書き出し・読み込み（学習状況タブ）

| ボタン | 出力 | 中身 |
|---|---|---|
| 📄 一覧をExcelで書き出す | `学習記録_YYYYMMDD.csv`（BOM付きUTF-8） | 漢字746行＋熟語4,558行。列は 種類／レベル／言葉・漢字／読み／意味／状態（覚えた・要復習・未学習）。Excelで直接開ける |
| 💾 バックアップを保存 | `学習記録_バックアップ_YYYYMMDD.json` | `jlpt-kanji-progress-v1`（漢字）・同 `-vocab`（熟語）・`jlpt-grammar-progress-v1`（文型）・`jlpt-dokkai-progress-v1`（読解）・`jlpt-choukai-progress-v1`（聴解）の**5システム分をまとめて**保存 |
| 📂 バックアップを読み込む | — | 上のファイルを選ぶと5キーを復元して再読み込み。**旧形式（漢字のみのJSON）も読める** |

- 記録の解除：詳細画面の「**Hapus tanda ／ クリア**」で1件ずつ「まだ記録なし」に戻せる（記録がないときは無効）。同じボタンの再押下による解除も従来どおり動く。漢字・熟語・文型の3システムに実装
- 要復習の一覧は学習状況タブに表示（漢字・熟語とも**全件**。初期表示は各200件で、「あと◯件を表示」ボタンで200件ずつ追加。総数は常に見出しに表示）
- 記録は端末内の `localStorage` のみ。サーバーには送らない



### 2.6-u N5漢字のレベル調整（2026-08-26）

データ元（kanji-data / open-anki-jlpt-decks）の分類ではN5が79字で、市販教材が目安とする「約100字」より少なかった。そこで、**体の部位と毎日使う基本の字23字**を N4（耳のみ N3）から **N5 に移した**。

| | |
|---|---|
| 移した字 | 手・目・口・足・耳・飲・買・売・立・多・少・安・新・古・空・会・社・田・町・道・週・曜・早 |
| 理由 | 介護の現場では、体の部位（手・目・口・足・耳）と「立つ」「飲む」を最初期に使う。JLPTは2010年以降**公式の漢字リストを公表していない**ため、レベル区分は各教材の判断による |
| 影響 | N5 79→**102**、N4 166→**144**、N3 368→**367**。総数746字は変わらない |
| 表示 | アプリのフッターに、移した23字と理由を明記している |

### 2.6-v レベル「記録」（申し送り記録の語彙）

施設から提供された介護記録・申し送りの実データをもとに整理した語彙群。**757語**。

| 項目 | 仕様 |
|---|---|
| 出所 | 施設の記録から作られた語彙リスト（Excel 4冊・のべ2,327行）。**記録の原文そのもの（利用者名・職員名・健康情報を含む2,151行＋3,882行）は取り込んでいない** |
| 個人情報 | 例文に混入していた実在の氏名2件は架空名（佐藤・山田）に置き換え済み。部屋番号・和暦日付の混入は検査済みで0件 |
| 分野 | 記録の言葉455／医療・症状135／薬・衛生材料100／体の部位61／記録の言い回し6 |
| 追加フィールド | `meaning_ja`（やさしい日本語の意味）を持ち、詳細画面に「日本語：〜」として表示。英訳がない語はこれを代用する |
| 薬剤の注意 | `caution` フィールドを持つ語（薬・衛生材料100語）は、詳細画面に赤枠で「職員が自分で判断して薬を使うことはできません。必ず看護師に確認します。」を日尼2言語で表示 |
| 例文 | 637語に記録由来の例文を付与。活用形で出る語は `[例文, 文中の形, その読み]` の3点形式 |
| 取り込み時の処理 | 列ずれ307行の修復（読みだけの行から漢字表記を復元）、重複1,100→757への集約、記録の生文180行の除外 |


### 2.6-t 手書き漢字検索（`tegaki.html`）

読めない漢字を枠に書いて調べる道具。**学習用ではなく、その場で引くための辞書**として位置づける。

| 項目 | 仕様 |
|---|---|
| 収録 | 漢字 **6,412字**・部首 287。学習用の746字より広く、記録・書類に出る字をほぼ拾える |
| データ | `tegaki-data.json` 約2.3 MB。`chars` / `npts`（画数）/ `radchars`・`radnames`（部首）/ `dict`（意味・画数・部首・成り立ち）/ `strokes` |
| 表示 | 読み・意味・画数・部首・成り立ち。Google翻訳へのリンクでインドネシア語訳にもつなげられる |
| 認識方式 | **オフライン**＝端末内で照合（通信なし）／**オンライン**＝Google手書き入力API（`inputtools.google.com`）に問い合わせ／**自動**＝オンライン優先、失敗時オフライン |
| 読み込み | このページを開いたときだけ 2.3 MB を読む。他の画面には影響しない |
| 学習記録 | なし（調べる道具のため） |

**通信について**：オンライン認識を選ぶと、**書いた線の座標が Google に送信される**。本システムで外部に通信するのはここだけであり、画面にもその旨を表示している。通信させたくない場合は「オフライン」を選ぶ。

### 2.6-r 新人コース（`shinjin/`）

N4を持って12月に入職した職員が、**翌年7月のN3に合格するまで**の入り口。
「今月やることだけを見ればよい」形にして、迷いをなくすことを狙っている。

| 項目 | 仕様 |
|---|---|
| 表示 | ①試験までの残り日数　②今月やること（3項目）　③**今月使うもの**（その月に必要なものだけ・レベル指定つき）　④そのほか（たたんである）　⑤7月までの道すじ（8段階、今月をオレンジで強調）　⑥職員用（診断テストへ） |
| 今月使うもの | `PLAN[i].use = [{k, lv}]` で月ごとに指定。`k` は k=漢字・語彙／g=文型／d=読解／c=聴解／n=100日／t=手書き。`lv` を書くと、**押したときに開く側のレベルを `localStorage` に書いてから移る**ので、そのレベルを選んだ状態で始まる。同じ `k` を違う `lv` で2つ並べてよい（12月は漢字・文型ともに N5 と N4 の2つ）。`use` を書かない月は6つ全部を出す |
| そのほか | `use` に入っていないものを `<details>` にたたんで下に置く。いつでも使える |
| レベルの受け渡し | 文型＝`jlpt-grammar-lastlv`、漢字・語彙＝`jlpt-kanji-lastlv`。どちらも**開いた側がそのレベルを覚える**仕組みを持っている（`kanji.html` にも今回追加した） |
| 計画データ | `PLAN` 配列（`start` / 月名 / ねらい / 目標 / やること3件）。**ここを直せば今月の表示と道すじの両方が変わる** |
| 試験日 | `var EXAM = new Date(2027, 6, 4)`（2027年7月の第1日曜を想定）。日程が確定したらここだけ直す |
| 今月の判定 | 今日が `start` を過ぎた最後の段を「今月」とする。開始前は先頭の段を出し「（コースが始まる前です）」と表示。試験日を過ぎたら残り日数の代わりに「おつかれさまでした」 |
| 導線 | 文型のリンクは、押したときに `jlpt-grammar-lastlv` に `N4` を書いてから遷移するので、**N4を選んだ状態で開く** |
| 学習記録 | このページ自体は記録を持たない。各システムの記録をそのまま使う |
| 合言葉 | 共通（`jlpt-kanji-gate-v1`）。入口で入れていれば聞かれない |

**スクリプトの並び順に注意（`shinjin/` と `shindan/` に共通）**：
データ定義（`PLAN` / `DATA` など）を、合言葉のブロックより**前**に置くこと。
合言葉のブロックは、すでに合言葉が保存されている端末では**即座に `init()` を呼ぶ**ため、
あとに置くとデータが未定義のまま走り、**画面は開くのに中身が出ない**という形で壊れる。
また、自動解錠を `try{...}catch(e){}` で囲まないこと。囲むとこの種の例外が黙って消え、原因が分からなくなる。
（2026-09-05：`shindan/` がこの形で壊れていたのを修正。合言葉を入れずに開くと動くため、
テストでは気づけなかった。**合言葉を保存ずみの状態でも確かめること**。）

### 2.6-s N4診断テスト（`shindan/` ＋ PDF 3点）

N4取得を前提に入職する職員の**実際の力**を確かめ、研修の順番を決めるための診断テスト。
**合否を判定するものではない。**

| 項目 | 仕様 |
|---|---|
| 構成 | 第1部 文字・語彙20問／第2部 文法15問／第3部 読解8問／第4部 聴解10問／第5部 書く12問。筆記50分＋聴解15分＋口頭5分 |
| セット | 第1回（入職直後）と第2回（3か月後）。**同じ構成・同じ難易度**で、伸びを比べられるようにしてある |
| 配点 | 73点満点。ただし判定は**部ごとの正答率**で行う（80%以上／60〜79%／60%未満の3段階） |
| 実施形態 | 筆記は紙（PDF）、聴解は `shindan/index.html` の読み上げ。音が出ないときは手引きのスクリプトを職員が読む |
| 聴解ページ | 合言葉は共通（`jlpt-kanji-gate-v1`）。第1回／第2回の切替、速さ2段階、**声の選択**、音のテスト、通し再生、1問ずつ再生、スクリプト表示。**学習者メニューには載せていない**（職員用） |
| 声の選択 | 端末の日本語音声を品質順（`GOOD` の並び）に出し、選んだ声を `localStorage` の `jlpt-shindan-voice` に**名前で**保存。次に開いたときに同じ声を選ぶ。選ぶと即座に試し読みする。聴解システム側も同じ仕組み（キーは `jlpt-choukai-voice`） |
| 音のテスト | 1文だけ読む確認用。①音が出るか ②音量合わせ ③iOSの自動再生制限の解除（初回のユーザー操作）を兼ねる。画面に用途を明記してある |
| 正解の位置 | 出題データは選択肢を種固定の乱数で並べ替えてから出力しているため、正解が①に偏らない |
| 生成 | `/tmp` の作業用スクリプトから問題冊子・手引き・聴解ページを一括生成。データは1か所（set1.js / set2.js）にあり、**問題冊子・正解一覧・聴解ページの3つが必ず一致する** |
| 口頭チェック | 5問（名前・来日／通勤経路／母国での学習／絵の描写／分からないときどうするか）と4観点の記入欄。**5番（聞き返せるか）を最重視**と手引きに明記 |

### 2.6-n N3合格100日コース（`n3.html`）

試験日から逆算し、**週ごと・曜日ごとにやることが決まっている**集中コース。7月不合格からの12月再受験など、期限が決まっている学習者向け。

| 項目 | 仕様 |
|---|---|
| 構成 | 週単位（現在**第14週**まで・設問1,226問）。各週に月〜日の日課。1日＝読解本文＋設問＋その本文から拾う語彙10語＋文法2項目＋確認ドリル6問 |
| 週の型 | 月＝短文（掲示・案内）／火＝短文（メール）／水＝中文（エッセイ）／木＝中文（意見文）／金＝情報検索／土＝読解通しテスト（第5〜7週55分／第8週以降は本番に近い50分）／日＝聴解7問＋今週の復習20問 |
| 週の追加 | `WEEKS` に週番号のキーを足すだけ。**週のボタンは `Object.keys(WEEKS)` から自動生成**されるため、他のコード変更は不要 |
| 学習記録 | `localStorage` の `n3-week1`〜（週ごと）と `n3-lastweek`（最後に開いた週）。**他の5システムとは別系統**で、バックアップには含まれない |
| 採点 | 「採点する」は**答えた問題だけ**を採点する。答えていない問題は<b>答えを見せず、そのまま答えられる</b>。答えたら、また「採点する」を押す |
| 採点の記録 | `state.mk`（問題ごと・キーは `曜日/問番号`）。`state.marked[曜日]` はその曜日を全部採点したときだけ true。古い記録（曜日単位しか持っていないもの）は `migrateMarks()` が問題ごとの形へ1回だけ移す |
| 採点ずみの問題 | 選び直せない。まだ採点していない問題は何度でも選び直せる |
| やり直し | 「この日をやり直す」で、その曜日の解答と採点状態の両方を消す |
| 合言葉 | 他システムと同じ `jlpt-kanji-gate-v1` / 同一ハッシュ。入口で入れれば聞かれない |
| 画面 | 上部に「☰ メニュー」。試験日までの残り日数を表示 |

### 2.6-w 施設の言葉の追加（アプリ内）

| 項目 | 仕様 |
|---|---|
| 保存先 | `localStorage` の `jlpt-kanji-mywords-v1`。**その端末にのみ残る** |
| 入力欄 | 言葉（必須）／ふりがな／**意味（インドネシア語）**／分野（選択）／小分類 |
| 翻訳 | **自動翻訳はしない。** インドネシア語は手入力。空のまま追加しようとすると確認ダイアログを出す |
| 訳なしの発見 | 熟語タブの記録フィルタに「**訳なし ／ Belum ada arti**」を用意。`meaning_id` が空の語だけを一覧できる |
| 修正 | 同じ言葉をもう一度追加すると上書き |
| 全体配布 | 「CSVを書き出す」で `vocab-extra.csv` 形式を出力 → GitHub の同ファイルに貼れば全員に反映 |


### 2.6-x 文型・文法システム（`bunpo/`）の収録数

| レベル | 項目数 | 内訳の考え方 |
|---|---|---|
| N5 | **76** | 名詞文・助詞（は・も・の・を・に・で・へ・と・から〜まで・や）・形容詞・動詞の各形・助数詞・時間・比較・基本の依頼 |
| N4 | **117** | 条件・推量・授受・敬語の入口・意志と予定・変化・目的・規則の言い方 |
| N3 | **150** | 逆接・関係・敬語・理由・程度など、書き言葉と正式な場面 |
| **記録** | **33** | 申し送り記録の書き方（〜あり／〜なし・〜が見られる・〜とのこと・〜にて・〜実施 など） |
| **合計** | **376** | 例文1,128・練習問題1,128 |

各項目に「形」「意味（尼・英）」「ポイント（日・尼・英）」「介護現場の例文3つ（読みがな・訳つき）」「練習問題3問（穴埋め2・並べ替え1）」を持つ。整合性は `bunpo/check.py` で機械的に検査する。

#### データの読み込み方（レベルごとに、必要になったときだけ）

`bunpo/grammar.json` は**目次**で、実データはレベルごとのファイルに分かれている。目次の `files` は次の形。

```json
"files": [
  { "file": "grammar-n5.json", "level": "N5", "count": 76 },
  { "file": "grammar-n4.json", "level": "N4", "count": 117 }
]
```

- **押されたレベルのファイルだけを、そのとき読む**（`ensureLevel()`）。一度読んだレベルは記憶するので、行き来しても待たされない
- 最後に見ていたレベルを `localStorage` の `jlpt-grammar-lastlv` に保存し、次に開いたときはそのレベルだけ読む。**初めての人は「すべて」**（＝全ファイル）
- 「すべて」チップ・レベル横断の検索・練習問題の「すべて」・**学習状況タブ**は全レベルが要るので、そこで残りを読む（`ensureAll()`）
- `count` は、**まだ読み込んでいないレベルの分母**を出すために使う。ファイルを編集したら `count` も直すこと（直し忘れると学習状況の分母だけずれる）
- 目次が旧形式（`files` がファイル名の文字列だけ）のときは、**今までどおり全部まとめて読む**ので、古いデータでも動く

効果（安いスマホ相当・CPU 4倍遅い設定で実測）：

| | 読み込み量 |
|---|---|
| 直す前（どの人も） | 1,145 KB |
| 直した後・初めての人 | 1,150 KB（変わらず） |
| 直した後・N5を勉強中の人 | **228 KB** |
| 直した後・N3を勉強中の人 | **596 KB** |
| 直した後・記録を勉強中の人 | **127 KB** |

N2など新しいレベルを足しても、**そのレベルを開かない人には届かない**。

### 2.6-y 読解システム（`dokkai/`）

JLPT本試験の読解セクションと同じ形式で、**文章を読んで4択で答える**練習をする独立システム。

| 項目 | 仕様 |
|---|---|
| 著作権 | **JLPT過去問の転載はしない。** 文章・設問・解説はすべてこのシステムのために書いたオリジナル。過去問（利用者提供のPDF）は形式の参考としてのみ使用 |
| 収録 | 文章45本・設問98問。N5 10本・N4 18本・N3 17本。分野は介護の現場28本・一般17本 |
| 形式 | **短文**14／**中文**11／**長文**5（N3）／**情報検索**15（シフト表・予定表・お知らせ一覧など） |
| 設問タイプ | **内容理解25・情報検索24・理由16・指示語10・内容一致8・主張8・語句の意味7**。市販ドリルの分類（指示代名詞／理由／情報検索）に合わせ、タイプ別に絞り込める |
| ふりがな | 本文・設問・選択肢に `漢字《かんじ》` 記法でルビを持ち、`<ruby>` で表示。ヘッダーの「ふりがな ON/OFF」で切替（設定は端末に保存） |
| 語注 | 各文章に難しい語のインドネシア語訳（本試験の「注」に相当） |
| 解説 | 全50問に「なぜその答えになるか」の日本語解説。答えた直後に表示 |
| 表の出題 | `table`（caption/headers/rows/note）で情報検索の表を組む。画像を使わないので軽く、文字も選択できる |
| 学習記録 | `localStorage` の `jlpt-dokkai-progress-v1` に `{文章id: {done, ok, total, res}}`。`res` は設問ごとの正誤（配列）。一覧に「済み」と正解数を表示 |
| 時間計測 | 文章を開くと自動で計測開始、全問答えると停止して `sec` に保存。目安（短文180秒・中文360秒・長文510秒・情報検索300秒。JLPT本試験の配分から算出）を超えると赤表示。「計測しない」で無効化できる |
| 読む速さの集計 | 学習状況に形式ごとの平均時間を目安と並べて表示。目安より遅い形式を赤で強調し、読み方の助言を出す。`sec` を持たない旧記録は集計対象外 |
| 弱点の診断 | 学習状況タブに**設問タイプ別の正解率**を表示。正解率の低い順に並べ、3問以上答えていて70%未満のタイプを赤で強調し、「このタイプだけ練習する」で該当文章に絞り込む。`res` を持たない旧記録は集計対象外（壊れない） |
| データ追加 | `dokkai/reading-nX.json` の `passages` に1件足すだけ。アプリの変更は不要 |


### 2.6-y1 聴解の画面まわり（実習生向けの案内）

聴解は操作の手順が多く、日本語だけでは伝わりにくいため、次を入れてある。

| 場所 | 内容 |
|---|---|
| `choukai/` 冒頭 | **「はじめての人へ ／ Cara pakai」**（`<details class="howto">`）。5手順を日尼併記。**初回は開いた状態**で、閉じると `localStorage` の `jlpt-choukai-howto` に覚える |
| 設定パネル | 「話す速さ／Kecepatan」「声／Suara」のように**ラベルを日尼併記**。速さボタンの補足も併記（pelan／agak cepat／normal／ujian） |
| 音のテスト | 用途を画面に明記（①音が出るか ②音量合わせ ③iOSの自動再生制限の解除） |
| 「聞く」ボタン | ボタン内に `Dengar`、回数表示を「聞いた回数 3／3×」に |
| 上部の得点 | `— / 48` では意味が取れないため `正解 — / 48` に |
| 声の数 | 「日本語の声：5つ（この端末に入っている声は全部で 23）」。**日本語の数を先に**出す |
| `n3.html` 日曜 | 同じ5手順の案内と **「🔊 音のテスト」を追加**（もとは無く、iPadで最初の音が出ない危険があった） |

**答えは押した瞬間に確定し、変更できない**（`state[pi][qi] !== null` で二度目を弾く）。
この仕様は画面の案内に赤字で明記してある。

### 2.6-y2 聴解システム（`choukai/`）

聞いて答える練習をする独立システム。**音声ファイルを持たず**、端末の音声合成（Web Speech API `speechSynthesis`）が読み上げるため、追加の通信量・容量は 0。

| 項目 | 仕様 |
|---|---|
| 著作権 | 場面の参考は『介護の日本語』（公益社団法人日本介護福祉士会）。**音声・設問・解説はこのシステムのために書き下ろしたオリジナル**。転載はしていない |
| 収録 | **8パート・48問**。〈介護の場面〉PART 1 申し送り6／PART 2 声かけの良し悪し6／PART 3 利用者さんの言葉5　〈JLPT形式〉PART 4 課題理解6／PART 5 ポイント理解6／PART 6 概要理解5／PART 7 発話表現6／PART 8 即時応答8 |
| JLPT形式 | 本試験の問題1〜5に対応。**課題理解**（このあと何をするか）／**ポイント理解**（質問を先に読み、必要な情報だけ聞く）／**概要理解**（何を伝えたいか）／**発話表現**（場面を聞いて言うことばを選ぶ・3択）／**即時応答**（短い問いかけに即答・3択） |
| 画面 | 上部に8パートへ跳ぶナビ。設問数は `QTOTAL` が自動で数える |
| 音声 | `SpeechSynthesisUtterance`（`lang='ja-JP'`）。数字は「二百二号室」のようにかな読みしやすい形で書き、画面のスクリプトでは「202号室」と表示する（`script` と `show` を分けて持つ） |
| 音声の質 | 端末に入っている日本語の声の中から、質の高いものを先に選ぶ（`vScore()`：Google 日本語・Kyoko・O-ren・Nanami など）。声は手動でも選べる |
| 読み上げ方 | 長い文は `chunks()` で「。？！」ごとに分け、1文ずつ `speak()` に渡す。途中で切れにくく、文の間（ま）も自然になる。**iPhone／iPad は分割すると停止することがあるため、1つにまとめて渡す** |
| アプリ内ブラウザ | LINE・Facebook・Instagram・Android WebView（`Line/`・`FBAN`・`; wv)` など）を UserAgent で判定し、日本語音声が見つからない場合に「Chromeで開いてください」の案内とURLコピーボタンを表示する。**Android のアプリ内ブラウザは `speechSynthesis.getVoices()` が常に空**で、読み上げが一切使えないため |
| スマホ対策 | ①最初のタップで無音の発話を1回流して音を「開ける」（iOSの自動再生制限）。②`cancel()` の直後の `speak()` は鳴らない端末があるため 60ms ずらす。③声の一覧が空でも `lang='ja-JP'` で読ませる（端末があとから声を返すことがある）。④5秒たっても鳴らないときは「音が出ませんか？」の案内を画面に出す |
| 速さ | 0.7（ゆっくり）／0.85（すこし速く）／**1.0（ふつう・既定）**／1.15（速い）の4段階。選んだ速さは `jlpt-choukai-rate` に保存し、次回も同じ速さで始まる。何回でも聞き直せ、聞いた回数を表示 |
| 出題 | 1つの音声に複数の設問（`sameAudioAs` で音声を共有）。JLPT聴解と同じく、答えた直後に正誤・日本語の解説・インドネシア語の要点・スクリプトを表示 |
| 学習記録 | `localStorage` の `jlpt-choukai-progress-v1` に `{"パート-問": 正誤}`（選んだ番号は `sel` に保存）。再訪時に答えた状態を復元し、ヘッダーに「正解 n / 17」を表示 |
| やり直し | パートごとの「もう一度 やる」はそのパートの記録だけを消す（他パートは残る） |
| 直接リンク | `choukai/index.html#part1 / #part2 / #part3` |
| データ追加 | `choukai/index.html` の `DATA` にパートや設問を足すだけ。総数は `QTOTAL` が自動で数える（入口の `CTOTAL` だけ手で合わせる） |

### 2.6-z 入口メニュー（`index.html`）

| 項目 | 仕様 |
|---|---|
| 役割 | 公開URLの直下。合言葉を入れ、**4つのシステムのメニューを全部並べて**表示する |
| メニュー | 6システム・計18項目。漢字側6項目（漢字一覧・熟語・フラッシュカード・練習問題・書き順・学習状況）／文型側3項目／読解側3項目（文章一覧・読む・学習状況）／聴解側3項目（申し送り・職員どうしの会話・利用者さんの言葉）の計15項目。押すと該当タブ・該当パートが直接ひらく |
| 直接リンク | `kanji.html#list/#vocab/#card/#quiz/#stroke/#stats`、`bunpo/index.html#list/#quiz/#stat`、`dokkai/index.html#list/#read/#stat`、`choukai/index.html#part1/#part2/#part3`。両アプリはURLのハッシュを読んで該当タブを開き（`openFromHash()`）、タブ切替時は `history.replaceState` でハッシュを書き戻す。**開いていたタブは再訪時に復元される** |
| 進み具合 | `localStorage` の `jlpt-kanji-progress-v1`（漢字）・同 `-vocab`（熟語）・`jlpt-grammar-progress-v1`（文型）・`jlpt-dokkai-progress-v1`（読解）・`jlpt-choukai-progress-v1`（聴解）から件数を数え、件数とバーで表示。端末内の記録のみを読むため通信は発生しない |
| その他 | 版（VERSION）の表示、Cache Storage を消して読み込み直す「🔄 最新にする」ボタン |
| 合言葉 | 保存キー・保存値をアプリ側と統一しているため、**入口で一度入れれば6システムとも開く** |

### 2.6-d 練習問題タブ（Latihan／JLPT形式）

JLPT本試験（言語知識・文字語彙）の**5つの大問形式すべて**に対応。

| 大問 | 出題のしかた | 誤答（ダミー）の作り方 |
|---|---|---|
| **問題1 漢字読み** | 例文の中の語に下線を引き、その読みを4択 | 正解の読みから**清濁・長短・促音・拗音**を1か所だけ変えた、本試験と同じまぎらわしい読み（例 けいちょう／けいちよう／けいぢょう／けいちょうう） |
| **問題2 表記** | 例文の該当部分をひらがなにし、正しい漢字表記を4択 | 語を構成する漢字を**同じ部首・同じ音読みの漢字**と入れ替えた、形のにた偽の熟語（例 満足／満束／浴足／漢足）。実在語と読みが重なるものは除外 |
| **問題3 文脈規定** | 例文の該当部分を（　　）にし、入る語を4択 | 同レベル・語長の近い語 |
| **問題4 言い換え類義** | 例文の語に下線を引き、意味がいちばん近い語を4択 | 意味が同じ語（同一の訳語をもつ語）を正解に、無関係な語をダミーに |
| **問題5 用法** | 語を1つ示し、その語の**使い方が正しい文**を4択 | 他の語の例文の該当部分を、その語に差し替えた誤用文 |
| （追加）漢字の読み | **その1字で語になる漢字**（山・水・手など275字）は1字のまま出題。**1字では使わない漢字**は2字熟語の中に置き、対象の字に下線を引いて**その字が読む部分だけ**を4択。相手の字が対象より上のレベル（未習・JLPT外）のときは**その字にふりがな**を付ける | 正解の読みを1か所だけ変えたまぎらわしい読み。作れないときは他の漢字の同じ長さの読み |

| 項目 | 仕様 |
|---|---|
| 例文データ | `sentences.json`（4,338語＝収録語彙4,558語の95%）。**介護197語・現場125語・N5 671語・N4 632語・N3 2,075語**をカバー。残る100語は「おはようございます」などの声かけフレーズ29語と、「～円」「～階」「～ばかり」「～観」などの接尾語・接尾表現71語で、それ自体が文・語尾のため対象外。値が文字列のときは見出し語がそのままの形で文中に出現、配列のときは `[例文, 文中の形, その読み]`（活用形に対応） |
| 例文がない語 | **自動的に旧形式**（「◯◯の読み方は？」など）で出題（フォールバック） |
| 出題範囲 | レベル（すべて/N5/N4/N3/介護/現場）× 問題数（10/20/30問） |
| 問題生成 | すべて端末内でその場で自動生成。サーバー負荷なし |
| 解答 | 選択直後に正誤を表示（正解＝緑、誤答＝赤）＋語の読み・意味を提示 |
| 採点 | 進捗バー・正解数／不正解数・最終得点（％）とコメント |
| 復習連携 | **間違えた語・漢字を自動で「要復習」に登録**。結果画面から「間違えた問題だけ」再挑戦可 |
| 品質担保 | 例文3,700件を機械検査（見出し語が文中に含まれるか／文中に1回だけ現れるか／読みがかなのみか／文の重複なし＝0件エラー）。さらに各形式600回の自動生成テストで、選択肢4つ・正解が選択肢に含まれる・正解が問題文に漏れていない・読みの選択肢がかなのみ・ダミーが正解と同じ読みでない、などを検証 |

**漢字の読み問題の内部処理**：2字熟語の読みから対象漢字の担当部分を切り出せたときだけ熟語形式を使います（`splitReading()`。音読み・訓読みの前方／後方一致で判定し、「発表（はっぴょう）」のような連濁・音便や「大人（おとな）」のような熟字訓は自動的に対象外）。ふりがなは、相手の字が漢字であること・相手のほうが上のレベルであること・ふりがなに答えが含まれないこと、の3条件を満たすときだけ表示します。

**例文を増やすには**：`sentences.json` の `sentences` に `"熟語": "例文"` を追加するだけです（活用する語は `"熟語": ["例文", "文中の形", "その読み"]`）。追加した語は次回から自動的に本試験形式で出題されます。

### 2.6-c 施設独自の専門用語（追加・変更）

| 方式 | 対象 | 仕組み |
|---|---|---|
| `vocab-extra.csv` | 全利用者 | Excel編集可のCSV（UTF-8）。アプリ起動時に読み込み、既存語の**上書き**・新規**追加**・`削除`欄での**非表示**に対応。GitHubへアップロードすれば1〜2分で全員に反映 |
| アプリ内追加 | その端末のみ | 熟語タブの「＋ 施設の言葉を追加する」から入力。`localStorage`（キー `jlpt-kanji-mywords-v1`）に保存。「CSVを書き出す」で `vocab-extra.csv` 形式に出力でき、そのまま全体配布へ移行できる |

**CSVの列**：`熟語, ふりがな, 意味（インドネシア語）, 分野, 小分類, 意味（英語）, 削除`
必須は「熟語」と「意味（インドネシア語）」のみ。分野は 介護／声かけ／医療／認知症／リハビリ／身体／施設／記録／安全／生活 など。
小分類は自由記入（例：食事・嚥下、排泄、移動のとき）。既存の小分類名を書けばその絞り込みに合流します。

### 2.7 言語切替

画面右上の `🌐 ID / EN` ボタンで、意味の表示をインドネシア語⇔英語に切替。選択は端末に記憶される。
UIの見出しは常にインドネシア語＋日本語の併記。

### 2.8 合言葉（パスワード）による閲覧制限

| 項目 | 仕様 |
|---|---|
| 動作 | サイトを開くと合言葉の入力画面を表示。正しく入力するまで漢字データを読み込まない |
| 現在の合言葉 | `owltiger2026` |
| 記憶 | 一度入力した端末は次回から入力不要（`localStorage` キー `jlpt-kanji-gate-v1`） |
| 実装 | 合言葉のSHA-256ハッシュを `index.html`（入口）・`kanji.html`・`bunpo/index.html` の `PASS_SHA256` に保持し、入力値のハッシュと照合。保存キーは3つとも `jlpt-kanji-gate-v1`、保存値もハッシュで統一しているため、**入口で一度入れれば両方のシステムが開く** |
| 変更方法 | 下記コマンドで新しいハッシュを作り、**3ファイルすべて**の `PASS_SHA256` を置き換えてプッシュ |

```bash
python3 -c "import hashlib;print(hashlib.sha256('新しい合言葉'.encode()).hexdigest())"
```

> **重要（保護の強さ）：** これは**部外者よけの目隠し**であり、暗号的な保護ではありません。
> ページのソースを読める人や、`kanji-data.json` のURLを直接開ける人にはデータが見えます。
> 本格的に保護する場合は Cloudflare Pages（無料・メール認証可）等への移設が必要です。

### 2.8-b 更新の反映（キャッシュ対策）

| 項目 | 仕様 |
|---|---|
| 配信方式 | 静的サイト（GitHub Pages）。**アプリストアの更新は不要**で、次回アクセス時に自動で新版を取得 |
| データ取得 | `kanji-data.json` `kanji-strokes.json` `vocab.json` `vocab-extra.csv` を `cache: 'no-cache'` で取得（毎回サーバーに更新確認、未更新なら304で軽量） |
| 版の表示 | フッターに `VERSION`（更新日）を表示。`kanji.html` の `const VERSION = 'YYYY-MM-DD'` を更新時に書き換える |
| 手動更新 | フッターの「🔄 最新に更新」ボタン。Cache Storage を破棄して再読み込み |
| HTML自体のキャッシュ | GitHub Pages の既定（約10分）。上記ボタンまたはブラウザ再読み込みで即時取得 |
| 学習記録 | 更新しても `localStorage` の記録は保持される |

### 2.9 学習記録の保存仕様

- 保存先：ブラウザの `localStorage`（キー名 `jlpt-kanji-progress-v1`）
- 形式：`{"漢字": "known" | "review", ...}`
- **端末・ブラウザごとに独立**。サーバーには一切送信されない
- ブラウザのデータ消去・シークレットモード終了で消える → 必要なら「書き出し」でバックアップ

---

## 3. データ仕様

### 3.1 `kanji-data.json`（漢字データ本体）

```json
{
  "name": "JLPT N5–N3 漢字データ",
  "levels": ["N5", "N4", "N3"],
  "count": 612,
  "languages": ["ja", "id", "en"],
  "sources": [ { "name": "...", "use": "...", "url": "...", "license": "..." } ],
  "kanji": [ { …1字ぶん… } ]
}
```

**漢字1字ぶんの構造**

| キー | 型 | 必須 | 内容 | 例 |
|---|---|---|---|---|
| `character` | 文字列 | ● | 漢字1字 | `"新"` |
| `level` | 文字列 | ● | JLPTレベル | `"N4"` |
| `strokes` | 数値 | ● | 画数 | `13` |
| `grade` | 数値/null | | 小学校で習う学年（中学以降はnull） | `2` |
| `freq` | 数値/null | | 新聞での使用頻度順位（1位＝最頻） | `147` |
| `on` | 配列 | ● | 音読み（ひらがな） | `["しん"]` |
| `kun` | 配列 | ● | 訓読み（送り仮名は `.` 区切り） | `["あたら.しい","あら.た"]` |
| `meanings` | 配列 | ● | 意味（英語） | `["new"]` |
| `meanings_id` | 配列 | ● | 意味（インドネシア語） | `["baru"]` |
| `radical` | オブジェクト | | 部首 `{character, name, meaning}` | `{"character":"斤","name":"おのづくり","meaning":"axe"}` |
| `examples` | 配列 | ● | 用例（下記） | |

**用例1件の構造**

| キー | 型 | 必須 | 内容 | 例 |
|---|---|---|---|---|
| `word` | 文字列 | ● | 単語 | `"新聞"` |
| `reading` | 文字列 | ● | 読み（ひらがな） | `"しんぶん"` |
| `meaning` | 文字列 | ● | 意味（英語） | `"newspaper"` |
| `meaning_id` | 文字列 | ● | 意味（インドネシア語） | `"koran, surat kabar"` |
| `jlpt` | 文字列 | | その語のJLPTレベル（該当時のみ） | `"N5"` |

> `meanings` と `meanings_id` は**件数が一致しなくてよい**（表示は各言語で独立して行うため）。

### 3.2 `kanji-strokes.json`（書き順データ）

```json
{ "viewBox": "0 0 109 109",
  "strokes": { "新": ["M12.5,24.8c...", "M9.75,38.5c...", … ] } }
```

- 漢字1字につき、**筆順どおりに並べた SVG パス文字列の配列**
- 座標系は 109×109（KanjiVG 標準）
- 配列の長さ＝画数（`kanji-data.json` の `strokes` と一致すること）

### 3.3 データの整合性ルール（検証項目）

1. `kanji-data.json` の全字が `kanji-strokes.json` に存在すること
2. `strokes`（画数）＝ 筆順パス配列の長さ
3. 全字に `on` または `kun` のいずれかが1件以上あること
4. 全字に `meanings`・`meanings_id`・`examples` が1件以上あること
5. 全用例に `meaning_id` があること
6. 用例の `word` にその漢字が含まれること

---

## 4. ファイルの状態（2026年7月27日 現在）

### 4.1 リポジトリ内のファイル

| ファイル | サイズ | 役割 | 状態 |
|---|---|---|---|
| `index.html` | 約12 KB | 入口メニュー（3システムの全メニューを一覧表示＋進み具合＋合言葉＋更新ボタン） | 最新 |
| `kanji.html` | 約40 KB | 漢字・言葉アプリ本体（HTML＋CSS＋JavaScript 単一ファイル） | 最新 |
| `kanji-data.json` | 約458 KB | 漢字612字のデータ | 最新 |
| `kanji-strokes.json` | 約460 KB | 書き順データ612字ぶん | 最新 |
| `vocab.json` | 約700 KB | 語彙4,558語（JLPT漢字語・かな語・カタカナ語＋介護＋現場） | 最新 |
| `vocab-extra.csv` | 1 KB未満 | 施設独自の語（Excelで編集して差し替え） | 雛形 |
| `README.md` | 約6 KB | 概要・使い方・出典 | 最新 |
| `SPEC.md` | 約21 KB | 仕様書（本書） | 最新 |
| `MANUAL.md` | 約14 KB | 使用説明書（配布用・運用手順・FAQ） | 最新 |
| `meeting-transcript.txt` | 約6 KB | 別プロジェクトの資料（本アプリとは無関係） | 既存のまま |
| `output/` | — | 別プロジェクトの出力（本アプリとは無関係） | 既存のまま |
| `.claude/skills/` | — | 別プロジェクトの設定（本アプリとは無関係） | 既存のまま |

> 旧版の `kanji-n3.json`（80字）は `kanji-data.json`（612字）に置き換えたため削除済み。

### 4.2 Git・公開の状態

| 項目 | 状態 |
|---|---|
| リポジトリ公開設定 | Public（GitHub Pages有効化のため） |
| 開発ブランチ | `claude/japanese-n3-kanji-AaKYe` |
| GitHub Pages 公開元 | 同ブランチの `/`（ルート） |
| 最新コミット | `a48466b` 使用説明書 MANUAL.md を追加 |
| 未コミットの変更 | なし |
| 閲覧制限 | 合言葉 `owltiger2026`（2.8参照） |

### 4.3 品質検証の記録

| 検証 | 方法 | 結果 |
|---|---|---|
| 画数の正確性 | KANJIDIC2・Kanji alive・KanjiVG の3独立ソースで照合 | 612字すべて一致（不一致0件） |
| データ欠損 | 上記3.3の6項目を自動チェック | 問題0件 |
| 画面動作 | Chromium実機での自動テスト83項目（合言葉ゲート・漢字表示・熟語表示・検索・絞り込み・アニメーション・言語切替・漢字↔熟語の相互移動・記録保存・スマホ表示・JSエラー） | 全項目合格 |
| インドネシア語訳 | 第三者レビューによる抽出検査（175字・866文字列） | 正確度 約99.3%、重大な誤り0件（指摘箇所は修正済み） |

---

## 5. 使い方

### 5.1 学習者（技能実習生）向け

1. スマホまたはPCのブラウザで **https://owlwindkaze-ux.github.io/owltiger1021/** を開く
2. 合言葉（会社から伝えられたもの）を入力する。次回からは省略されます
3. ホーム画面に追加しておくとアプリのように使える（iPhone：共有→ホーム画面に追加／Android：メニュー→ホーム画面に追加）
4. おすすめの学習手順
   - **一覧タブ**で漢字を眺め、気になる字をタップ → 書き順アニメと用例を確認
   - **フラッシュカードタブ**で「N5・未学習」から開始。毎日10〜15分
   - 間違えた字は自動で「要復習」に入るので、翌日「要復習」だけを復習
   - **書き順タブ**を見ながら、週1回は紙に書いて練習
   - **学習状況タブ**で到達率を確認

### 5.2 管理者（導入・運用担当）向け

**日常運用：** 何もする必要はありません（静的サイトのため無停止・無保守）。

**手元で動かして確認・編集する場合：**

```bash
git clone https://github.com/owlwindkaze-ux/owltiger1021.git
cd owltiger1021
git checkout claude/japanese-n3-kanji-AaKYe
python -m http.server 8000
# ブラウザで http://localhost:8000 を開く
```

> ファイルをダブルクリックで直接開くとJSONを読み込めません。必ず上記のローカルサーバー経由で開いてください。

**修正を公開サイトに反映する：**

```bash
git add -A
git commit -m "修正内容の説明"
git push origin claude/japanese-n3-kanji-AaKYe
# 1〜2分でGitHub Pagesに自動反映
```

### 5.3 漢字を1字だけ追加・修正する

`kanji-data.json` の `kanji` 配列に、3.1の形式で1件追加します。

```json
{
  "character": "橋", "level": "N2", "strokes": 16,
  "grade": 3, "freq": 1174,
  "on": ["きょう"], "kun": ["はし"],
  "meanings": ["bridge"], "meanings_id": ["jembatan"],
  "radical": { "character": "木", "name": "きへん", "meaning": "tree" },
  "examples": [
    { "word": "歩道橋", "reading": "ほどうきょう", "meaning": "pedestrian bridge",
      "meaning_id": "jembatan penyeberangan", "jlpt": "N2" }
  ]
}
```

書き順も付ける場合は、`kanji-strokes.json` の `strokes` に筆順どおりのSVGパス配列を追加します（取得方法は6.2）。
書き順データが無い字は、書き順の図が空欄になるだけで他の機能は正常に動きます。

---

## 6. N2レベルを追加する方法

### 6.1 追加規模の見積り（確認済みの実データ）

| 項目 | 数値 |
|---|---|
| 追加される漢字 | **367字**（N2） |
| 追加後の合計 | **979字**（N5 102＋N4 144＋N3 367＋N2 367＋介護 133のうち重複を除く） |
| 用例データの入手可能率 | 367字中**366字**（`被` のみ用例データなし → 手入力が必要） |
| 書き順データ | 367字すべて取得可能 |
| データ量の増加 | 約1.6倍（合計 約1.5 MB。スマホでも問題ない範囲） |
| 翻訳が必要な文字列 | 約2,900語（意味＋用例のインドネシア語訳） |

### 6.2 手順（技術担当者向け）

**ステップ1：元データを取得する**

```bash
mkdir -p ~/kanji-build && cd ~/kanji-build
# ① 読み・意味・画数・JLPTレベル（KANJIDIC2由来）
curl -sSLo kanji-data-src.json https://raw.githubusercontent.com/davidluzgouveia/kanji-data/master/kanji.json
# ② 用例・部首（Kanji alive）
curl -sSLo ka_data.csv https://raw.githubusercontent.com/kanjialive/kanji-data-media/master/language-data/ka_data.csv
# ③ 語彙のJLPTレベル（用例にN2タグを付けるため）
curl -sSLo jlpt_n2.csv https://raw.githubusercontent.com/jamsinclair/open-anki-jlpt-decks/main/src/n2.csv
```

**ステップ2：N2漢字の書き順データを取得する**

```bash
python3 -c "
import json
d=json.load(open('kanji-data-src.json',encoding='utf-8'))
t=[k for k,v in d.items() if v.get('jlpt_new')==2]
open('urls.txt','w').write('\n'.join(
  'https://raw.githubusercontent.com/KanjiVG/kanjivg/master/kanji/%05x.svg'%ord(k) for k in t))
print(len(t),'字')"
mkdir -p kvg && cd kvg && xargs -P 10 -n 1 curl -sS -O < ../urls.txt && ls | wc -l   # 367 になればOK
```

**ステップ3：データを組み立てて既存ファイルに追記する**

- `jlpt_new == 2` の漢字を抽出し、3.1の形式に変換して `kanji-data.json` の `kanji` 配列へ追加
- 各SVGから `id="kvg:XXXXX-sN"` を持つ `<path>` の `d` 属性を**N順に並べて**取り出し、`kanji-strokes.json` の `strokes` に追加
- `count` を `979`、`levels` を `["N5","N4","N3","N2"]` に更新
- 用例は Kanji alive の `examples` 列（`単語（よみ）` 形式）から1字あたり4語まで採用し、`jlpt_n2.csv` に載っている語には `"jlpt": "N2"` を付ける
- `被` は用例が無いため手動で追加（例：`被害（ひがい）`／`被る（こうむる）`）

**ステップ4：インドネシア語訳を作る（品質上もっとも重要）**

> **必ず「日本語の文脈ごと」訳すこと。** 英語の意味だけを見て訳すと同形異義語で誤訳が発生します。
> 実際に起きた例：権利→`kanan`（右）／現在→`hadiah`（贈り物）／表→`meja`（机）／産→`beruang`（熊）。
> 翻訳を依頼（またはAIに指示）する際は、**漢字・読み・用例の日本語をセットで渡し**、
> 「英語ではなく日本語の意味から訳すこと」を明示してください。

- `meanings_id`（意味）と各用例の `meaning_id` を作成
- 表記は標準インドネシア語（Bahasa Indonesia baku）。マレー語表現・俗語は不可
- 動詞は `berjalan` のように動詞形（`untuk` は付けない）、助数詞は `penghitung untuk 〜`

**ステップ5：アプリ側でN2を選べるようにする（`kanji.html` の6か所）**

| 行 | 変更内容 |
|---|---|
| 一覧タブのレベルボタン | `<button class="chip" data-lv="N2">N2</button>` を追加 |
| 書き順タブのレベルボタン | 同上 |
| フラッシュカードのレベル選択 | `<option value="N2">N2</option>` を追加 |
| ヘッダーの字数表示 | `cnt('N2')` を追加 |
| 並べ替えのレベル順 | `{ N5:0, N4:1, N3:2 }` → `{ N5:0, N4:1, N3:2, N2:3 }` |
| 学習状況タブ | `['N5','N4','N3']` → `['N5','N4','N3','N2']` |

あわせて、タイトル・説明文・フッターの字数（612字）を979字に更新します。

**ステップ6：検証してから公開する**

```bash
# データ整合性（3.3の6項目）
python3 -c "
import json
d=json.load(open('kanji-data.json',encoding='utf-8'))
s=json.load(open('kanji-strokes.json',encoding='utf-8'))['strokes']
bad=[k['character'] for k in d['kanji']
     if len(s.get(k['character'],[]))!=k['strokes']
     or not (k['on'] or k['kun']) or not k['meanings_id'] or not k['examples']
     or any(not e.get('meaning_id') for e in k['examples'])]
print('総数',len(d['kanji']),'／ 不備',len(bad),bad[:10])"
```

- 画面の動作確認：一覧・検索・N2絞り込み・書き順アニメ・フラッシュカード・スマホ表示
- 翻訳の抜き取り検査（100件程度）。特に同形異義語（right／letter／present／table／bear／store など）を重点的に
- 問題がなければ `git add -A && git commit && git push`

### 6.3 N1まで広げる場合

同じ手順で `jlpt_new == 1`（**1,232字**）を追加でき、全レベル合計は**2,211字**になります。
ただしデータ量が約3.5 MBになるため、その段階では
「レベルごとにJSONを分割し、必要なレベルだけ読み込む」構成に変えることを推奨します。

---

## 7. データ出典とライセンス

| データ | 用途 | ライセンス |
|---|---|---|
| [kanji-data](https://github.com/davidluzgouveia/kanji-data)（KANJIDIC2 / EDRDG 由来） | 読み・意味・画数・学年・頻度・JLPTレベル | CC BY-SA 4.0 |
| [Kanji alive](https://github.com/kanjialive/kanji-data-media)（University of Chicago） | 用例・部首 | CC BY 4.0 |
| [KanjiVG](http://kanjivg.tagaini.net)（Ulrich Apel） | 書き順（筆画データ） | CC BY-SA 3.0 |
| [open-anki-jlpt-decks](https://github.com/jamsinclair/open-anki-jlpt-decks)（tanos.co.uk 由来） | 用例のJLPTレベル判定 | CC BY 4.0 |

- `kanji-strokes.json` は KanjiVG の派生物のため **CC BY-SA 3.0** で提供されます（再配布時は同ライセンスと出典表示が必要）
- インドネシア語訳はAIで作成し、第三者レビューで検査したものです

> **JLPTのレベル区分について：** 日本語能力試験は2010年の新形式移行以降、**公式の漢字リストを公表していません**。
> 本ツールのレベル区分は広く使われているデータセットの分類に基づく目安であり、教材によってN2/N3の境界が異なる字があります
> （例：`橋` は本データではN2扱い）。

---

## 8. 制約事項・既知の注意点

| 項目 | 内容 |
|---|---|
| インターネット接続 | 必須（オフライン非対応） |
| 学習記録 | 端末・ブラウザごとに独立。機種変更時は引き継がれない（「書き出し」で手動バックアップ可） |
| 複数人での共有 | 同時利用は何人でも可。ただし学習記録は共有されない（個人単位） |
| 対応ブラウザ | Chrome・Safari・Edge・Firefox の最新版。Internet Explorer 非対応 |
| 合言葉の強度 | 部外者よけの目隠しレベル。ソースやJSONのURLを直接開ける人には見える（2.8参照） |
| 音声読み上げ | 未対応（将来の拡張候補） |
| 手書き練習の判定 | 未対応（書き順の提示のみ） |
